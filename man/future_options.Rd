% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/future_options.R
\name{future_options}
\alias{future_options}
\title{Get or Set Future-Related Options}
\usage{
future_options(
  globals_maxsize = NULL,
  rng_onmisuse = NULL,
  globals_onreference = NULL,
  resolve_recursive = NULL,
  gc = NULL,
  stdout = NULL,
  conditions = NULL,
  fork_enable = NULL,
  verbose = NULL
)
}
\arguments{
\item{globals_maxsize}{Numeric maximum size of globals in GiB, or \code{NULL}
to leave unchanged. Converted to bytes for \code{future.globals.maxSize}.}

\item{rng_onmisuse}{Character action when non-parallel-safe RNG is detected.
One of \code{"ignore"}, \code{"warning"}, \code{"error"}, or \code{NULL} to leave unchanged.}

\item{globals_onreference}{Character action when globals contain non-
exportable references (connections, external pointers, environments).
One of \code{"ignore"}, \code{"warning"}, \code{"error"}, or \code{NULL}.}

\item{resolve_recursive}{Integer or logical controlling recursive resolution
depth for futures, or \code{NULL}.}

\item{gc}{Logical. If \code{TRUE}, trigger garbage collection on the worker after
resolving a future. Reduces memory pressure at a small speed cost.
Or \code{NULL} to leave unchanged.}

\item{stdout}{Logical or \code{NA}. \code{TRUE} captures stdout from workers, \code{FALSE}
discards it, \code{NA} skips capture entirely. Or \code{NULL} to leave unchanged.}

\item{conditions}{Character pattern of conditions to relay from workers
(e.g., \code{"message"}, \code{"warning"}), or \code{NULL}.}

\item{fork_enable}{Logical. If \code{FALSE}, disables forked processing even
on supported platforms. Or \code{NULL} to leave unchanged.}

\item{verbose}{Logical. If \code{TRUE} (default when called with no arguments),
prints the current option values.}
}
\value{
A named list of all future option values (invisibly).
}
\description{
A unified interface for managing all \verb{future.*} options with human-friendly
names and validation. When called with no arguments, displays all current
settings. Memory sizes use GiB (not raw bytes), and invalid combinations
produce warnings.
}
\examples{
\dontrun{
# View current settings
future_options()

# Set memory limit and RNG checking in one call
future_options(globals_maxsize = 8, rng_onmisuse = "warning")

# Strict mode: error on references and RNG misuse
future_options(globals_onreference = "error", rng_onmisuse = "error")

# Production mode: capture everything, GC aggressively
future_options(gc = TRUE, stdout = TRUE, rng_onmisuse = "error")
}
}
