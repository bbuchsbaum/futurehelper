% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_capabilities.R
\name{parallel_capabilities}
\alias{parallel_capabilities}
\title{System Parallel Capabilities Report}
\usage{
parallel_capabilities(verbose = TRUE)
}
\arguments{
\item{verbose}{Logical. If \code{TRUE} (default), prints the report. If \code{FALSE},
returns the info list silently.}
}
\value{
A list (invisibly) with components:
\describe{
\item{\code{system}}{OS, R version, platform, PID.}
\item{\code{cores}}{Available and total system cores.}
\item{\code{ram_gib}}{Total system RAM in GiB (or \code{NA}).}
\item{\code{fork_safe}}{Fork-safety assessment.}
\item{\code{hpc}}{Detected HPC scheduler and job info.}
\item{\code{backends}}{Data frame of installed/available backends.}
\item{\code{plan}}{Current plan info (from \code{\link[=plan_info]{plan_info()}}).}
\item{\code{options}}{Current future options (from \code{\link[=future_options]{future_options()}}).}
\item{\code{issues}}{Character vector of potential issues.}
}
}
\description{
Prints a comprehensive assessment of the current system's parallel
computing capabilities: available backends, CPU/memory resources, fork
safety, HPC scheduler detection, and potential issues. Useful for
debugging setup problems, onboarding new users, and pasting into
bug reports.
}
\examples{
parallel_capabilities()

# Capture without printing
caps <- parallel_capabilities(verbose = FALSE)
caps$backends
caps$cores
}
